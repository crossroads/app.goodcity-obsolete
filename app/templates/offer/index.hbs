<p>
Offer Items({{itemCount}})
</p>

{{#if itemCount }}

  {{#if isDraft}}
    <button  {{action "addItem"}}>Add Item</button>
    {{#link-to 'offer.confirm'}}All Done, Next{{/link-to}}
  {{else}}
    {{#link-to 'offer.review_status'}}Review Status{{/link-to}}
  {{/if}}

  <p><button {{action 'cancelOffer' this}} class='removeItem'>Cancel Offer</button><p>

  <p>
    <table cellspacing="20">
      <tr><th>Description</th><th>Condition</th></tr>
      {{#each items}}
        <tr>
          <td><img {{bind-attr src=defaultImage }}></td>
          <td>
            <div class='item_name'>
              {{#link-to 'item' this}}{{donorDescription}}{{/link-to}}
            </div>
          <br/>{{donorCondition.name}}</td>
          <td>{{#link-to 'item.edit' this}}Edit{{/link-to}}</td>
          <td><button {{action 'removeItem' this}} class='removeItem'>Remove</button></td>
        </tr>
      {{/each}}
    </table>
  </p>

{{else}}

  <div>{{t offer.no_items}}</div>
  <button {{action "addItem"}}>Add Item</button>

{{/if}}
